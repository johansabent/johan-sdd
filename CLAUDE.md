# CLAUDE.md — johan-sdd

@AGENTS.md

Use the same repository contracts, worktree ownership, authority gates, and verification evidence
as every other agent. `using-johan-skills` is automatic; propose user-invoked routes instead of
silently invoking them.
